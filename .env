# This is checked into source control, so put sensitive values into `.env.local`
#
# Required for production, generated at boot in dev/test if missing:
#   SESSION_SECRET=<bundle exec rake secret | head -c 64>
# Newsletter:
#   EMAIL_OCTOPUS_API_KEY=
#   EMAIL_OCTOPUS_LIST_ID=
# Observability:
#   SENTRY_DSN=
#   PLAUSIBLE_DOMAIN=fluck.site
