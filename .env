# This is checked into source control, so put sensitive values into `.env.local`
#
# Required for production (Rack requires ≥64 chars), generated at boot in
# dev/test if missing:
#   SESSION_SECRET=$(ruby -rsecurerandom -e 'puts SecureRandom.hex(64)')
# Newsletter:
#   EMAIL_OCTOPUS_API_KEY=
#   EMAIL_OCTOPUS_LIST_ID=
# Observability:
#   SENTRY_DSN=
#   PLAUSIBLE_DOMAIN=fluck.site
